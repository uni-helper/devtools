{
  "extends": "../../tsconfig.json", // 继承根目录下的 tsconfig.json 配置
  "compilerOptions": {
    "baseUrl": ".", // 基础目录是当前目录（src/client）
    "paths": {
      "~/*": ["./*"] // 映射到当前目录下的所有文件
    }
  },
  "include": [
    "./*"
  ],
  "exclude": [
    "node_modules"
  ]
}
